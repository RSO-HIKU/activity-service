apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "activity-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "activity-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "activity-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "activity-service.selectorLabels" . | nindent 8 }}
    spec:
      {{- if .Values.azure.enabled }}
      volumes:
        - name: secrets-store
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: {{ include "activity-service.fullname" . }}-azure-kv
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.clusterIP.port }}
          {{- if .Values.azure.enabled }}
          volumeMounts:
            - name: secrets-store
              mountPath: /mnt/secrets-store
              readOnly: true
          command:
            - sh
            - -c
            - |
              set -euo pipefail

              # Read secrets from CSI mounted files
              export KUMULUZEE_DATASOURCES_DEFAULT_USERNAME=$(cat /mnt/secrets-store/activity-db-user)
              export KUMULUZEE_DATASOURCES_DEFAULT_PASSWORD=$(cat /mnt/secrets-store/activity-db-password)
              export DB_HOST=$(cat /mnt/secrets-store/pg-host)
              export KUMULUZEE_DATASOURCES_DEFAULT_CONNECTIONURL="jdbc:postgresql://${DB_HOST}:{{ .Values.azure.port }}/{{ .Values.azure.database }}?sslmode={{ .Values.azure.sslmode }}"
              
              # Run the Java application
              exec java -jar /app/activity-service.jar
          {{- end }}
          env:
            - name: KUMULUZEE_ENV_NAME
              value: {{ .Values.environment | default "dev" | quote }}
            {{- if .Values.database.enabled }}
            # Dev: use Kubernetes secret with KumuluzEE env var names
            - name: KUMULUZEE_DATASOURCES_DEFAULT_CONNECTIONURL
              value: "jdbc:postgresql://{{ .Values.database.host }}:{{ .Values.database.port }}/{{ .Values.database.name }}"
            - name: KUMULUZEE_DATASOURCES_DEFAULT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ include "activity-service.fullname" . }}-db
                  key: DB_USER
            - name: KUMULUZEE_DATASOURCES_DEFAULT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "activity-service.fullname" . }}-db
                  key: DB_PASS
            {{- end }}
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}